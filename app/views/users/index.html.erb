<table>
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Actions</th>
    </tr>
    <% @users.each do |user| %>
        <tr>
            <th><%= user.name %></th>
            <th><%= user.email %></th>
            <th><%= link_to "View Profile", user_path(user), class: "button small" %></th>
        </tr>
    <% end %>
</table>

