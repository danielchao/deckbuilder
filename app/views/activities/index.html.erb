<h1>The Social Stack</h1>

<% @activities.each do |activity| %>
    <p><%= time_ago_in_words(activity.created_at) %> ago </p>
    <p class="activity">
        <%= link_to activity.owner.name, activity.owner if activity.owner %>
        <%= render_activity activity %>
    </p>
<% end %>
